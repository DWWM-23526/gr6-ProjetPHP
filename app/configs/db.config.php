<?php

    const DB_HOST = 'localhost';
    const DB_NAME = 'sitefilm';
    const DB_USER = 'root';
    // const DB_PASSWORD = 'ad^&5447883';

        // //Constantes d'environnement
        // define("DBHOST", "localhost");
        // define("DBUSER", "root");
        // define("DBPASS", "");
        // define("DBNAME", "sitefilm");

        //DSN(data source name) de connection
        // $dsn = "mysql:dbname=".DBNAME.";host=".DBHOST;

        // //On va se connecter a la base
        // try{
        //     //On instancie PDO
        //     $db = new PDO($dsn, DBUSER, DBPASS);

        //     //On s'assurer d'envoyer les donnees en UTF8
        //     $db->exec("SET NAMES utf8");

        //     //On definie le mode fetch par default
        //     $db->setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE, PDO::FETCH_ASSOC);
            
        // }catch(PDOException $e){
        //     die("Erreur:".$e->getMessage());
        // };
?>